# HDF5-Visualizer

This repo contains the code for the HDF5 file app visualizer for human studies at the ARoS Lab at NC State.

The code was developed using Python 3.12.3 with the provided packages using the `requirements.txt` file. To run th app, you can just execute the following command after making the appropriate environment active:

```
streamlit run Dashboard.py
```
